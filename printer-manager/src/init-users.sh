#!bash/bin

# Initialize users
ls /home >> ./users

# At the end of each line related to each user add the monthly cote.
# cote: a cote is the number of <pages> a user can print.
# <pages> contain 3600 characters
awk '{}'
